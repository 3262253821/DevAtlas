# DevAtlas 系统架构图

## 1. 当前 MVP 架构

DevAtlas 采用模块化单体架构：前端、FastAPI 后端、MySQL、Chroma、文件系统和 DeepSeek 在本机协同运行。当前不引入微服务、消息队列、Redis 或 Agent 编排。

```mermaid
flowchart LR
    Browser[浏览器]

    subgraph Frontend[Vue 3 前端]
        Views[Views 页面]
        Router[Vue Router 路由守卫]
        Store[Pinia 登录状态]
        Axios[Axios API 实例]
        SSE[fetch + ReadableStream SSE 解析]
    end

    subgraph Backend[FastAPI 模块化单体]
        AuthRouter[auth router]
        KBRouter[knowledge_base router]
        DocRouter[documents router]
        RetrievalRouter[retrieval router]
        QARouter[qa router]
        IncidentRouter[incidents router]

        Schemas[Pydantic schemas]
        Dependencies[JWT / owner 权限依赖]
        Services[业务 services]
        Ingestion[文档解析 / 切分 / 入库]
        Retrieval[Embedding / Chroma 检索]
        LLM[DeepSeek LLM Service]
    end

    MySQL[(MySQL 8)]
    Chroma[(Chroma Persistent Client)]
    Files[(上传文件目录)]
    DeepSeek[DeepSeek API]

    Browser --> Views
    Views --> Router
    Views --> Store
    Views --> Axios
    Views --> SSE

    Axios --> AuthRouter
    Axios --> KBRouter
    Axios --> DocRouter
    Axios --> RetrievalRouter
    SSE --> QARouter
    SSE --> IncidentRouter

    AuthRouter --> Schemas
    KBRouter --> Schemas
    DocRouter --> Schemas
    QARouter --> Schemas
    IncidentRouter --> Schemas

    AuthRouter --> Dependencies
    KBRouter --> Dependencies
    DocRouter --> Dependencies
    RetrievalRouter --> Dependencies
    QARouter --> Dependencies
    IncidentRouter --> Dependencies

    AuthRouter --> Services
    KBRouter --> Services
    DocRouter --> Services
    RetrievalRouter --> Retrieval
    QARouter --> Retrieval
    QARouter --> LLM
    IncidentRouter --> Retrieval
    IncidentRouter --> LLM
    DocRouter --> Ingestion

    Services --> MySQL
    Ingestion --> Files
    Ingestion --> Chroma
    Ingestion --> MySQL
    Retrieval --> Chroma
    Retrieval --> MySQL
    LLM --> DeepSeek

    QARouter -. token/citation/done/error .-> SSE
    IncidentRouter -. token/citation/done/error .-> SSE
```

## 2. 后端目录职责

```text
backend/app/core
  配置、JWT、当前用户依赖、文件安全

backend/app/db
  SQLAlchemy Base、Engine、Session

backend/app/models
  users、knowledge_bases、documents、versions、chunks、incidents

backend/app/schemas
  请求参数和响应结构

backend/app/routers
  HTTP 路由、鉴权依赖和状态码转换

backend/app/services
  文档处理、Embedding、Chroma、检索、LLM 和 incident 业务
```

## 3. 文档入库调用链

```mermaid
sequenceDiagram
    participant U as 浏览器
    participant R as documents router
    participant S as file_storage
    participant I as document_ingestion
    participant E as embedding
    participant V as vector_store
    participant DB as MySQL
    participant C as Chroma

    U->>R: multipart 上传文件
    R->>R: JWT + knowledge_base owner 校验
    R->>S: 保存文件并计算 SHA-256
    S-->>R: SavedFile
    R->>I: ingest_document()
    I->>DB: 创建/查找逻辑文档和版本
    I->>I: 解析文件并切分文本
    I->>E: 生成文档块向量
    E-->>I: embeddings
    I->>C: upsert vectors + metadata
    I->>DB: 保存 chunks，版本更新为 indexed
    R-->>U: 上传结果、版本状态和 chunk_count
```

## 4. RAG 普通问答调用链

```mermaid
sequenceDiagram
    participant U as 浏览器
    participant R as qa router
    participant T as retrieval service
    participant C as Chroma
    participant DB as MySQL
    participant L as DeepSeek

    U->>R: question + top_k
    R->>R: JWT + knowledge_base owner 校验
    R->>T: retrieve_context()
    T->>DB: 找 indexed 版本
    T->>C: question embedding + metadata filter + Top-K
    C-->>T: 文档块、距离和 metadata
    T-->>R: context + sources
    R->>L: RAG Prompt
    L-->>R: token stream
    R-->>U: token → citation → done
```

## 5. 关键边界

- 前端路由守卫只改善用户体验，最终权限必须由后端 `current_user` 和 `owner_id` 再次校验；
- MySQL 保存业务事实，Chroma 只作为语义检索索引；
- 只有 `indexed` 版本允许进入默认检索；
- SSE 建立后，业务错误通过 `error` 事件传递，不能只看 HTTP 200；
- T14 只实现 MVP 主链路，不包含问答历史会话、Redis、Agent、Reranker 和 Docker 部署。
